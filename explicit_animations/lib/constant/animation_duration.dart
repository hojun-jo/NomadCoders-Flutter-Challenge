const Duration animationDuration = Duration(milliseconds: 50);
const Duration reverseDuration = Duration(milliseconds: 800);
const Duration delayDuration = Duration(milliseconds: 200);
const Duration intervalDuration = Duration(seconds: 2);
